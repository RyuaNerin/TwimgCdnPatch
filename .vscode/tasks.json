{
	"version": "2.0.0",	
	"type": "shell",	
	"echoCommand": true,
	"cwd": "${workspaceFolder}",
	"tasks": [
		{
			"label": "build",
			"command": "go build ${file}",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		},		
	]
}
